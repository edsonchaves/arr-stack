# Calibre-Web Setup

**URL:** `http://localhost:8083`

- Default credentials: `admin` / `admin123` — change immediately after first login
- Admin panel → Set database path to `/books` (the mounted book library)
- Admin panel → Enable the ebook viewer for in-browser reading
